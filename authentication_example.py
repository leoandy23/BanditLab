# Función crítica sin autenticación (vulnerable por falta de autenticación)
def critical_function():
    # Simulación de una función crítica que debería requerir autenticación
    print("Critical function executed")


# Ejemplo de uso
if __name__ == "__main__":
    critical_function()
